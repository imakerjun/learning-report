# CLAUDE.md — learning-report

내 학습이 **어디로 움직이는가**를 한 장으로 보는 노션 스타일 대시보드 템플릿.
빈도가 아니라 **변화량(drift)** 이 학습 신호다.

## 이 저장소에서 Claude Code가 하는 일

사용자가 "내 학습 데이터 해석해줘 / 피드백 남겨줘 / 리포트 만들어줘"라고 하면,
**[AGENT.md](AGENT.md)의 절차와 규칙을 그대로 따른다.** 핵심만 옮기면:

1. 사용자의 학습 활동 출처(작업 저장소 git 로그, 메모, 학습 일지 등)에서 토픽별 빈도를 센다.
2. `data/report.json`을 생성한다(없으면 `data/sample.json` 형식 참고). **`topics`만 있으면 화면은 동작한다.**
3. **해석과 피드백을 직접 쓴다 — 이게 Claude Code가 더하는 핵심 가치다.**
   - `readings`: 에릭슨(의도적 수련) · 비요크(다시 꺼내 쓰기) 두 렌즈로 현재 상태를 읽고(`reading`),
     각 렌즈에서 **오늘·내일 할 수 있는 작은 행동 하나**(`feedback`)를 남긴다.
   - `narrative`: 지난 한 주 학습 서사(선택).
4. 로컬에서 확인: `./serve.sh` → `http://localhost:8080`
5. (공유 시) `?reveal=1`로 퀴즈 게이트를 건너뛴 화면을 스크린샷한다.

## 절대 규칙 (받아쓰기 금지)

- **차트를 문자로 옮겨 적지 않는다.** "8개 중 2개 새로 등장" 같은 건 카드에 이미 있다.
  사실을 *연결*하고(쏠림·동반 이동), 가장 튀는 지점에 *강약*을 주고, *질문*으로 착지한다.
- **합성 점수를 지어내지 않는다.** "학습 강도 0.72" ❌. 모든 값은 실제 빈도 카운트.
- **렌즈는 말투가 아니라 '무엇을 보는가'를 바꾼다.** 에릭슨은 수련의 질, 비요크는 다시 꺼내 썼는가.
  둘이 같은 사실을 다른 톤으로 반복하면 실패다.
- **실존 인물은 원칙으로만 빌리고 가짜 인용을 만들지 않는다.** "비요크가 말하길" ❌ → "다시 꺼내 쓰기 관점으로 보면" ⭕.
- **`reading`(상태 관찰)과 `feedback`(다음 행동)을 분리한다.** feedback은 추상 조언이 아니라 바로 할 수 있는 한 가지.

## 데이터 스키마

`data/sample.json`이 곧 스펙이다. 자세한 필드 설명은 [AGENT.md](AGENT.md) 참고.

## 설계 철학

1. **변화량이 본체** — 토픽은 빈도가 아니라 변화량 순으로 정렬된다. 못 보는 건 "사라진 것".
2. **보기 전에 예측** — 첫 화면은 예측 퀴즈. 예측과 실제의 갭이 곧 "내가 나를 얼마나 아는가".
3. **에이전트는 답이 아니라 읽기·질문·다음 한 걸음을 준다** — 결론을 대신 내리지 않는다.

## 커밋

- 단일 HTML 템플릿. 빌드 없음. CDN은 폰트(Pretendard) 하나뿐.
- `data/report.json`은 `.gitignore` 대상(참여자별 데이터는 로컬에만).
